# .env

# Use SQLite for local development
DATABASE_URL="file:./dev.db"

# NextAuth.js Configuration
NEXTAUTH_URL="http://localhost:3000"
# Generate a better secret for development (but keep the production one secret!)
# You can generate one here: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET="your-better-local-development-secret-key"

# --- Production DATABASE_URL (Commented out for local dev) ---
# For Vercel deployment, set this in Vercel Environment Variables dashboard
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"
